#include <iostream>
#include <string>
#include <vector>
using std::cout;
using std::cin;
using std::endl;
using std::getline;
using std::string;
using std::vector;

int main (void){
  string first, second;
  while (getline(cin, first)){
    getline(cin, second);
    vector<int> a,b;
    
    for (string::iterator itr = first.begin(); itr != first.end(); itr++)
      a.push_back(*itr);

    for (string::iterator itr = second.begin(); itr != second.end(); itr++)
      b.push_back(*itr);
  }
  return 0;
}
